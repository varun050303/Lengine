{
  "schema_version": "2.0",
  "mechanism_id": "mechanism.trust-boundary-confusion",
  "display_name": "Trust-Boundary Confusion",
  "status": "approved",

  "core_principle": "Attacker-controlled input causes a privileged component to act on the attacker's behalf, crossing a trust boundary that should separate the attacker from the privileged capability. The component has authority the attacker lacks — network position, file system access, credentials, elevated permissions, or the ability to produce output that other parties treat as authoritative. The attacker does not gain the component's identity; they gain the component's capability by controlling either what it does or what it produces.",

  "minimum_causal_signature": {
    "components_involved": [
      "Untrusted Caller / External Actor",
      "Privileged Server / Dispatcher",
      "Protected Resource / Internal Subsystem, OR a downstream trusting party who consumes the component's output"
    ],
    "authority_transition": {
      "case_a_privileged_execution": "Attacker supplies a destination, command, or resource path; the privileged component executes the operation using its own ambient authority (network position, local socket, or IAM role), granting privileged execution to untrusted intent.",
      "case_b_trusted_output": "Attacker supplies output-shaping metadata; the privileged component embeds the untrusted value into generated content, cache keys, or routing decisions that a separate trusting party later consumes as if it were authoritative.",
      "shared_failure": "In both cases the component treats attacker-supplied data as if it carried the authority of a validated source."
    },
    "security_decision_bypassed": "The protected destination or output relies on ambient positional trust (evaluating caller source address, caller process identity, or raw request metadata) rather than authenticating or validating the initiator's data against a known-good source.",
    "failure_condition": "Attacker-controlled data determines the target, operation, or embedded content of a privileged sink without strict allowlist validation, enabling the server's ambient authority — or its output's assumed trustworthiness — to be exerted on, or extended to, arbitrary attacker-chosen values."
  },

  "conditions": [
    "A component has authority the attacker lacks (network position, credentials, file access, elevated permissions)",
    "Attacker-controlled input influences what the privileged component does (destination, command, resource path)",
    "The trust boundary between the attacker and the capability is not enforced at the input layer — the component acts on the input without verifying whether the attacker should have the resulting capability"
  ],

  "members": ["topic.ssrf", "topic.xxe", "topic.host-header-attacks"],

  "investigation_question": "Can attacker-controlled input cause a privileged component to perform an action the attacker could not perform directly, or to produce output that a separate trusting party will act on as if it were authoritative? What capability does the attacker gain — and what authority or trust relationship makes that capability possible?",

  "defensive_implication": "Validate attacker-controlled input against a strict allowlist before passing it to any privileged component, and before embedding it in any output that other parties will trust (generated URLs, cache keys, routing decisions). The validation must constrain the action or output to only what the attacker is authorized to trigger or influence. Apply defense-in-depth at the infrastructure layer — assume the application-level validation may be bypassed and limit the privileged component's own capabilities (network segmentation, least-privilege IAM roles, disabled metadata endpoints) or its output's blast radius (short-lived tokens, cache-key completeness).",

  "common_manifestations": [
    "SSRF: Attacker controls the destination of a server-side HTTP request, gaining the server's network position",
    "XXE with external entities: XML parser fetches attacker-controlled URLs using the server's file/network access",
    "PDF generators that fetch attacker-controlled URLs using the server's network position",
    "Headless browsers (Chromium, wkhtmltopdf) rendering attacker-controlled URLs from the server's network",
    "Host header attacks: Attacker-controlled Host/X-Forwarded-Host header is embedded unvalidated into generated URLs (e.g. password-reset links) or cache keys, causing a third party — not the attacker — to trust attacker-influenced content"
  ],

  "related_concepts": [
    "concept.url-parsing",
    "concept.dns-resolution",
    "concept.http-redirects",
    "concept.network-trust-boundaries"
  ],

  "sources": [
    {
      "id": "S1",
      "title": "PortSwigger - Server-side request forgery (SSRF)",
      "url": "https://portswigger.net/web-security/ssrf",
      "tier": 2,
      "claim": "SSRF attacks exploit trust relationships to escalate an attack from the vulnerable application and perform unauthorized actions, leveraging trust relationships with the server or back-end systems",
      "retrieved_this_pass": true
    },
    {
      "id": "S2",
      "title": "OWASP - Server Side Request Forgery",
      "url": "https://owasp.org/www-community/attacks/Server_Side_Request_Forgery",
      "tier": 2,
      "claim": "SSRF allows an attacker to abuse server functionality to perform requests to internal/external resources on behalf of the server",
      "retrieved_this_pass": true
    },
    {
      "id": "S3",
      "title": "PortSwigger Web Security Academy — Password reset poisoning",
      "url": "https://portswigger.net/web-security/host-header/exploiting/password-reset-poisoning",
      "tier": 2,
      "claim": "Documents password reset poisoning as an attacker manipulating a vulnerable website into generating a password-reset link pointing to an attacker-controlled domain by controlling the Host header used to build that link — the output-generation sub-case of trust-boundary confusion, distinct from SSRF's network-fetch sub-case",
      "retrieved_this_pass": true
    }
  ]
}
